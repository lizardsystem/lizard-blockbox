language: python
python:
  - 2.6
  - 2.7
env:
  - DJANGO=1.2.7
  - DJANGO=1.3.1
  - DJANGO=1.4
install:
  - pip install pep8 --use-mirrors
  - python bootstrap.py 
script:
  - bin/buildout 
  - pep8 --exclude=migrations --ignore=E501,E225 lizard_blockbox
